import * as types from '@playbooks/types';
export declare const Nav: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.NavProps) => import('react/jsx-runtime').JSX.Element;
export declare const NavHeader: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.NavHeaderProps) => import('react/jsx-runtime').JSX.Element;
export declare const NavTitle: ({
	name,
	size,
	tailwind,
	className,
	children,
	...props
}: types.NavTitleProps) => import('react/jsx-runtime').JSX.Element;
export declare const NavBody: ({
	name,
	size,
	tailwind,
	className,
	children,
	...props
}: types.NavBodyProps) => import('react/jsx-runtime').JSX.Element;
export declare const NavList: ({
	tailwind,
	children,
	...props
}: types.NavListProps) => import('react/jsx-runtime').JSX.Element;
export declare const NavItem: ({
	tailwind,
	children,
	...props
}: types.NavItemProps) => import('react/jsx-runtime').JSX.Element;
export declare const NavBtn: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.NavBtnProps) => import('react/jsx-runtime').JSX.Element;
export declare const NavLink: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.NavLinkProps) => import('react/jsx-runtime').JSX.Element;
