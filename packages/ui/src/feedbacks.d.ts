import * as types from '@playbooks/types';
export declare const Feedback: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.FeedbackProps) => import('react/jsx-runtime').JSX.Element;
export declare const FeedbackHeader: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.FeedbackHeaderProps) => import('react/jsx-runtime').JSX.Element;
export declare const FeedbackIcon: ({
	name,
	type,
	icon,
	tailwind,
	className,
	...props
}: types.FeedbackIconProps) => import('react/jsx-runtime').JSX.Element;
export declare const FeedbackBody: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.FeedbackBodyProps) => import('react/jsx-runtime').JSX.Element;
export declare const FeedbackPretitle: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.FeedbackTitleProps) => import('react/jsx-runtime').JSX.Element;
export declare const FeedbackTitle: ({
	name,
	size,
	tailwind,
	className,
	children,
	...props
}: types.FeedbackTitleProps) => import('react/jsx-runtime').JSX.Element;
export declare const FeedbackText: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.FeedbackTextProps) => import('react/jsx-runtime').JSX.Element;
export declare const FeedbackActions: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.FeedbackActionsProps) => import('react/jsx-runtime').JSX.Element;
