import * as types from '@playbooks/types';
export declare const Oval: ({
	name,
	tailwind,
	className,
	...props
}: types.SpinnerProps) => import('react/jsx-runtime').JSX.Element;
export declare const Puff: ({
	name,
	tailwind,
	className,
	...props
}: types.SpinnerProps) => import('react/jsx-runtime').JSX.Element;
