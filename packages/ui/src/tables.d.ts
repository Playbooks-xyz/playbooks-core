import * as types from '@playbooks/types';
export declare const Table: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.TableProps) => import('react/jsx-runtime').JSX.Element;
export declare const TableHeader: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.TableHeaderProps) => import('react/jsx-runtime').JSX.Element;
export declare const TableHeaderRow: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.TableHeaderRowProps) => import('react/jsx-runtime').JSX.Element;
export declare const TableHead: ({
	name,
	alt,
	value,
	params,
	setParams,
	tailwind,
	className,
	children,
	...props
}: types.TableHeadProps) => import('react/jsx-runtime').JSX.Element;
export declare const TableBody: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.TableBodyProps) => import('react/jsx-runtime').JSX.Element;
export declare const TableRow: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.TableRowProps) => import('react/jsx-runtime').JSX.Element;
export declare const TableData: ({
	name,
	title,
	value,
	tailwind,
	className,
	children,
	...props
}: types.TableDataProps) => import('react/jsx-runtime').JSX.Element;
