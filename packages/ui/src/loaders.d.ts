import * as types from '@playbooks/types';
export declare const ModalLoader: ({
	name,
	title,
	message,
	tailwind,
	className,
	children,
	...props
}: types.PageLoaderProps) => import('react/jsx-runtime').JSX.Element;
export declare const PageLoader: ({
	name,
	title,
	message,
	tailwind,
	className,
	children,
	...props
}: types.PageLoaderProps) => import('react/jsx-runtime').JSX.Element;
export declare const SectionLoader: ({
	name,
	title,
	message,
	tailwind,
	className,
	children,
	...props
}: types.SectionLoaderProps) => import('react/jsx-runtime').JSX.Element;
