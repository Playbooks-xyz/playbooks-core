import * as types from '@playbooks/types';
export declare const Link: (props: any) => import('react/jsx-runtime').JSX.Element;
export declare const PrimaryLink: ({
	name,
	size,
	alt,
	active,
	className,
	...props
}: types.LinkProps) => import('react/jsx-runtime').JSX.Element;
export declare const AccentLink: ({
	name,
	size,
	active,
	className,
	...props
}: types.LinkProps) => import('react/jsx-runtime').JSX.Element;
export declare const BorderLink: ({
	name,
	size,
	active,
	className,
	...props
}: types.LinkProps) => import('react/jsx-runtime').JSX.Element;
export declare const TabLink: ({
	name,
	size,
	active,
	className,
	...props
}: types.LinkProps) => import('react/jsx-runtime').JSX.Element;
export declare const TextLink: ({
	name,
	size,
	active,
	className,
	...props
}: types.LinkProps) => import('react/jsx-runtime').JSX.Element;
export declare const LinkShared: ({
	alt,
	prevIcon,
	prevImg,
	icon,
	img,
	nextIcon,
	nextImg,
	taskRunning,
	tailwind,
	children,
	...props
}: types.LinkProps) => import('react/jsx-runtime').JSX.Element;
export declare const LinkWrapper: ({
	name,
	alt,
	href,
	target,
	disabled,
	tailwind,
	className,
	children,
	...props
}: types.LinkProps) => import('react/jsx-runtime').JSX.Element;
