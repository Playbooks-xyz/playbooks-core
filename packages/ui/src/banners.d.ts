import * as types from '@playbooks/types';
export declare const Banner: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.BannerProps) => import('react/jsx-runtime').JSX.Element;
export declare const BannerIcon: ({
	name,
	icon,
	tailwind,
	className,
	...props
}: types.BannerIconProps) => import('react/jsx-runtime').JSX.Element;
export declare const BannerBody: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.BannerBodyProps) => import('react/jsx-runtime').JSX.Element;
export declare const BannerTitle: ({
	name,
	size,
	tailwind,
	className,
	children,
	...props
}: types.BannerTitleProps) => import('react/jsx-runtime').JSX.Element;
export declare const BannerText: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.BannerTextProps) => import('react/jsx-runtime').JSX.Element;
export declare const BannerActions: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.BannerActionsProps) => import('react/jsx-runtime').JSX.Element;
