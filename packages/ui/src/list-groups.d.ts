import * as types from '@playbooks/types';
export declare const ListGroup: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.LiPropsstGroup) => import('react/jsx-runtime').JSX.Element;
export declare const ListItem: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.LiPropsstItem) => import('react/jsx-runtime').JSX.Element;
