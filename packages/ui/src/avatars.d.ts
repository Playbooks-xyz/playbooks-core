import * as types from '@playbooks/types';
export declare const Avatar: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.AvatarProps) => import('react/jsx-runtime').JSX.Element;
export declare const AvatarBadge: ({
	name,
	size,
	tailwind,
	className,
	children,
	...props
}: types.AvatarBadgeProps) => import('react/jsx-runtime').JSX.Element;
export declare const AvatarImg: ({
	name,
	alt,
	size,
	tailwind,
	className,
	...props
}: types.AvatarImgProps) => import('react/jsx-runtime').JSX.Element;
export declare const AvatarBody: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.AvatarBodyProps) => import('react/jsx-runtime').JSX.Element;
export declare const AvatarTitle: ({
	name,
	size,
	tailwind,
	className,
	children,
	...props
}: types.AvatarTitleProps) => import('react/jsx-runtime').JSX.Element;
export declare const AvatarText: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.AvatarTextProps) => import('react/jsx-runtime').JSX.Element;
export declare const AvatarActions: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.AvatarActionsProps) => import('react/jsx-runtime').JSX.Element;
