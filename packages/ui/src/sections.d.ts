import * as types from '@playbooks/types';
export declare const Section: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.SectionProps) => import('react/jsx-runtime').JSX.Element;
export declare const SectionHeader: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.SectionHeaderProps) => import('react/jsx-runtime').JSX.Element;
export declare const SectionPretitle: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.SectionPretitleProps) => import('react/jsx-runtime').JSX.Element;
export declare const SectionTitle: ({
	name,
	size,
	tailwind,
	className,
	children,
	...props
}: types.SectionTitleProps) => import('react/jsx-runtime').JSX.Element;
export declare const SectionSubtitle: ({
	name,
	size,
	tailwind,
	className,
	children,
	...props
}: types.SectionSubtitleProps) => import('react/jsx-runtime').JSX.Element;
export declare const SectionText: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.SectionTextProps) => import('react/jsx-runtime').JSX.Element;
export declare const SectionActions: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.SectionActionsProps) => import('react/jsx-runtime').JSX.Element;
export declare const SectionBody: ({
	name,
	size,
	tailwind,
	className,
	children,
	...props
}: types.SectionBodyProps) => import('react/jsx-runtime').JSX.Element;
export declare const SectionHr: ({
	name,
	tailwind,
	className,
	...props
}: types.SectionHrProps) => import('react/jsx-runtime').JSX.Element;
export declare const SectionFooter: ({
	name,
	tailwind,
	className,
	children,
	...props
}: types.SectionFooterProps) => import('react/jsx-runtime').JSX.Element;
