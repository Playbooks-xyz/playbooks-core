import { getCode, getName, getNames } from 'country-list';

export const getCountryCode = getCode;

export const getCountryName = getName;

export const getCountryNames = getNames;

// Docs
// https://www.npmjs.com/package/country-list
